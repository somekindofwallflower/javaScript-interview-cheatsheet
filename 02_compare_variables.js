"use strict";

// There are two ways of comparing variables in js

let a = 5;
let b = "5";

console.log(a == b); // true because it checks only for value
console.log(a === b); // false because it checks for value and type
